/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'example.com', 'res.cloudinary.com'],
  },
  // Add other Next.js config options here as needed
};

module.exports = nextConfig;
